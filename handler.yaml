# Istio handler configuration for Apigee gRPC adapter for Mixer
# generated by apigee-istio provision on 2020-01-13 12:44:22
apiVersion: config.istio.io/v1alpha2
kind: handler
metadata:
  name: apigee-handler
  namespace: apigee
spec:
  adapter: apigee
  connection:
    address: apigee-adapter.apigee:5000
  params:
    customer_base: CHANGE
    hybrid_config: /opt/apigee/customer/default.properties
    org_name: CHANGE
    env_name: CHANGE
    key: CHANGE
    secret: CHANGE
    analytics:
      collection_interval: 10s
